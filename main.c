#include <stdio.h>

int main() {
    printf("Hello, World!\n");
    return 0;
}

int fgetMatrix(int adMatrix){
    // Reads Node counts and Matrix from file.
    FILE *map;
    if (map = fopen("graphData", "r+") != NULL){
        while (!feof(map)){

        }
    }


}